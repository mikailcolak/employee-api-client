# employee-api-client

This project created for [https://github.com/mikailcolak/employee-api] project.

## Downloading node packages
`yarn install`

## Run
`yarn start`

## Build
`yarn build`

After `yarn build` completed. The application files on `./build/` folder can be
deployed/served on preferred web server.
